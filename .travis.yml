language: go
  
go:
  - 1.8
  - 1.9

sudo: required

services: postgresql

before_install: export GOPATH=$TRAVIS_BUILD_DIR

before_script:
  - go get github.com/lib/pq
  - psql -c 'create database bucket;' -U postgres

script:
  - go test -v crawler
  - go test -v twse
  - go test -v bucket -user postgres
